version: "3.8"

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: laravel-app
    container_name: laravel_app
    restart: always
    ports:
      - "8001:80"
    volumes:
      - .:/var/www/html
      - ./storage:/var/www/html/storage
    environment:
      APP_ENV: local
      APP_DEBUG: "true"
      APP_KEY: "base64:HdKUqzT18RozZGVC8qa7eNqMzx/zKxwwkmblXQebBHw="
      APP_URL: http://localhost:8000
      DB_HOST: "0.0.0.0"
